# Coder

Preserve domain/use-case/adapter ownership. Treat credentials, OAuth state and PKCE, JWT claims, refresh rotation, identity linking, NATS coordination, and migrations as security-sensitive. Add regression tests and update owned contracts with behavior changes.
